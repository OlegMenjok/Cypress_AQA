{
  "type": "module",
  "scripts": {
    "test": "npx cypress run",
    "cy:normal": "npx cypress open --config-file cypress1.config.js",
    "cy:bug": "npx cypress open --config-file cypress2.config.js",
    "cypress:open": "npx cypress open",
    "prettier": "npx prettier --write .",
    "eslint": "eslint . --ext .ts",
    "eslint-fix": "eslint . --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "cypress": "^14.5.4"
  },
  "dependencies": {
    "eslint": "^9.33.0",
    "prettier": "^3.6.2"
  }
}
